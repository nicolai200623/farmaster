# VPS-friendly requirements
# pandas-ta is now OPTIONAL - bot will use manual calculation if not available

# Core dependencies (all required)
python-binance==1.0.19
torch>=2.0.0
pandas>=2.0.0
numpy>=1.24.0
python-dotenv>=1.0.0
python-telegram-bot>=20.0
requests>=2.31.0
scikit-learn>=1.3.0

# Optional: pandas-ta (recommended for better performance)
# If you can install git on VPS:
# pip install git+https://github.com/twopirllc/pandas-ta.git
#
# If git doesn't work, bot will use manual indicator calculation (slower but works)

