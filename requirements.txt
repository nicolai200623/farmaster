# Core dependencies
python-binance==1.0.19
torch>=2.0.0
pandas>=2.0.0
numpy>=1.24.0
python-dotenv>=1.0.0
python-telegram-bot>=20.0
requests>=2.31.0
scikit-learn>=1.3.0

# Optional: pandas-ta (recommended but not required)
# Install with: pip install git+https://github.com/twopirllc/pandas-ta.git
# If not installed, bot will use manual indicator calculation

# Optional: TA-Lib (requires manual installation on Windows)
# ta-lib==0.4.28

